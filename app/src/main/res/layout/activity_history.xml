<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activity.HistoryActivity">

    <ImageView
        android:layout_marginTop="15dp"
        android:layout_marginLeft="20dp"
        android:src="@drawable/ic_back"
        android:id="@+id/back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <ImageView
        android:layout_marginTop="20dp"
        android:layout_gravity="center"
        android:src="@drawable/bgsplassh"
        android:layout_width="114dp"
        android:layout_height="60dp" />

    <TextView
        android:layout_marginTop="29dp"
        android:textColor="@color/dark"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:text="Nhập để tìm kiếm hoặc chọn 1 trong các tỉnh thành dưới đây"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></TextView>

    <androidx.appcompat.widget.SearchView
        android:layout_marginTop="10dp"
        android:hint="Nhập để tìm kiếm"
        android:id="@+id/searchView"
        android:background="@drawable/customed"
        app:queryHint="Nhập để tìm kiếm"
        app:iconifiedByDefault="false"
        app:searchIcon="@drawable/ic_next"
        android:layout_width="match_parent"
        android:layout_marginRight="20dp"
        android:layout_marginLeft="20dp"
        android:layout_height="40dp" />
    <ListView

        android:id="@+id/lvHistory"
        android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_height="match_parent">

    </ListView>
</LinearLayout>